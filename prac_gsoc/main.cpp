#include<iostream>
#include<vector>
#include<stdio.h>
#include<string.h>
#include<strings.h>
#include<stdio.h>
#include<string>
using namespace std;
int main()
{
    string h,k;
    cin>>h>>k;
    if(h.equal(k))
        cout<<"r";
   /* vector<string>l1;
    int i,n,t;
    char s[100];
    cin>>t;
    while(t--)
    {
        cin>>n;
        for(i = 0;i <= n;++i)
        {
            gets(s);
            l1.push_back(s);
        }
       // for(i = 0;i <= n;++i)
        cout<<l1[0]<<endl;
    }*/
}
